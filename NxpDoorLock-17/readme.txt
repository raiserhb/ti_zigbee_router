NxpDoorLock-9_2  ——在NxpDoorLock-9的基础上修改心跳上报时的长度

NxpDoorLock-9_2  ——在9_2的基础上上报心跳

NxpDoorLock-10  ——配置心跳重启后不恢复默认20分钟心跳

NxpDoorLock-10_1 --针对纳泽修改，电量上报真实值、心跳重启、重发(若心跳唤醒无数据将直接休眠)、一次性接收30条缓存、请求网关缓存和清除网关缓存顺序(应在标准工程上修改)
NxpDoorLock-10_2 --心跳或手动唤醒只有先唤醒的方式才接受密码，避免重复发送。配置心跳后16小时重启计数

NxpDoorLock-11 -- 修改清缓存流程（注意如果门锁发送0x74指令不发送给网关）

NxpDoorLock-12 -- 默认心跳时的重启计数(在設置心跳時修改重启计数或者在重启时修改重启计数)、心跳和手动唤醒同时接收数据只发一次(即唤醒后除非断开，否则cluster0x5000只发一次)
					增加逻辑，如果唤醒后无法进入休眠逻辑，重启模块--20190526

NxpDoorLock-13 --去除多余的data request

NxpDoorLock-14 --OTA重启发送announce、OTA远程通知OTA门锁OTA升级、两次获取网关缓存数据网关无响应第三次发送announce

NxpDoorLock-15 --限制透传、可入3.0网关、OTA网关回复Response后不会死循环请求(3次后不在请求OTA文件)、下发指纹通知录入

NxpDoorLock-16 --入网退网3.0处理management leave(此工程注意查看0x410C是否调用正确，上一个版本限制透传可能有问题)

NxpDoorLock-16-1 --针对自如门锁做透传

NxpDoorLock-17 --异常耗电，原因应用层有任务定时超过了休眠的时间，导致休眠处理出异常(特别在重启时)